"""Pytest configuration for the Nacka Energi custom integration.

The presence of this file at the repository root puts the root on ``sys.path``
so tests can import the integration as ``custom_components.nacka_energi``.
The ``pytest-homeassistant-custom-component`` plugin (a dev dependency) is
auto-loaded via its entry point and provides the Home Assistant test harness.
"""
