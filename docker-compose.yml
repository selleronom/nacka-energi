services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: homeassistant
    restart: unless-stopped
    ports:
      - 8123:8123
    volumes:
      - ./ha-config:/config
      - ./custom_components/nacka_energi:/config/custom_components/nacka_energi
